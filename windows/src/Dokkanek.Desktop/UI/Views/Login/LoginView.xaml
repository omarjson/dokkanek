<UserControl x:Class="Dokkanek.Desktop.UI.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft">
  <Grid>
    <Border Style="{StaticResource Card}" MaxWidth="420" HorizontalAlignment="Center" VerticalAlignment="Center">
      <StackPanel>
        <TextBlock Text="تسجيل الدخول" Style="{StaticResource PageTitle}" />
        <TextBlock Text="ادخل بحسابك للمتابعة" Style="{StaticResource PageSub}" Margin="0,0,0,12" />
        <TextBlock Text="اسم المستخدم" FontWeight="Bold" />
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" MinHeight="40" Margin="0,4,0,8" />
        <TextBlock Text="كلمة المرور" FontWeight="Bold" />
        <PasswordBox x:Name="PassBox" MinHeight="40" Margin="0,4,0,8" />
        <TextBlock Text="{Binding Error}" Foreground="Red" Margin="0,0,0,8" />
        <Button Content="دخول" Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PassBox, Path=Password}"
                Style="{StaticResource PrimaryBtn}" MinHeight="40" />
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
